This is Perl module Email::Address::JP::Mobile.

INSTALLATION

Email::Address::JP::Mobile installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan Email::Address::JP::Mobile

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Email::Address::JP::Mobile documentation is available as in POD. So you can do:

    % perldoc Email::Address::JP::Mobile

to read the documentation online with your favorite pager.

Naoki Tomita
